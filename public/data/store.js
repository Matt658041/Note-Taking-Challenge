const fs = require('fs');
const util = require(`util`);

// need to add a class store. which is going to read the db .json file. data your going to have a write and your going to calling get notes add notes and remove.need a promise statement and a stringify statement. 
